quote = "to be or not to be"

# strings are immutable in python -- meaning they can't be changed 
# But you can reassign them to a new value
quote2 = quote.replace('be', 'me')
print(quote.upper())
print(quote.capitalize())
print(quote2)
